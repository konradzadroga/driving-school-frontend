export interface Course {
    id: number;
    name: string;
    description: string;
    places: number;
  }

  export interface CourseDTO {
    id: number;
    name: string;
    description: string;
    places: number;
  }